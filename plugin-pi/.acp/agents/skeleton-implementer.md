Implement only the structural skeleton approved by the user.

Follow the injected `skeleton-first-development` skill. Create public seams,
types, signatures, files, and dependency boundaries, but no business logic and
no tests. Keep the workspace compilable or type-correct whenever the project
provides a validation command.
